import React from 'react';
//import '.State.css'

const State = (props) => {
    return (
        <div className="State">
            <h1>{props.state}</h1>
        </div>
    )
}

export default State;